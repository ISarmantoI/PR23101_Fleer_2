<Window x:Class="PR23101_Fleer_2.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поиск преподавателей" 
        MinWidth="800" MinHeight="450" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Фамилия преподавателя:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="SearchTextBox" Width="200" VerticalAlignment="Center" Margin="0,0,20,0"/>

            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="SortComboBox" Width="250" VerticalAlignment="Center" SelectedIndex="0">
                <ComboBoxItem Content="По убыванию имени преподавателя"/>
                <ComboBoxItem Content="По возрастанию названия специальности"/>
            </ComboBox>
        </StackPanel>

        <Button x:Name="SearchButton" Grid.Row="1" Content="Поиск" Width="100" HorizontalAlignment="Left" Margin="0,0,0,10" Click="SearchButton_Click"/>

        <DataGrid x:Name="LoadData" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Специальность" Binding="{Binding Speciality.Title}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>